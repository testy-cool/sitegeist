/**
 * Replaced at build time by scripts/build.mjs with the short commit the bundle was
 * built from, suffixed "-dirty" when the tree had uncommitted changes, or "unknown"
 * when built outside a git checkout.
 */
declare const __BUILD_STAMP__: string;
